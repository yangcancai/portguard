#!/bin/sh /etc/rc.common

START=60
USE_PROCD=1

PROG=/usr/sbin/fwknopd

start_service() {
	procd_open_instance
	procd_set_param command "$PROG" -f
	procd_set_param respawn
	procd_close_instance
}

reload_service() {
	"$PROG" -R
}

stop_service() {
	"$PROG" -K
}
